import { type Theme } from '../types';

export const emptyTheme: Theme = { colors: {}, fonts: {} };
export const lightTheme: Theme = { colors: {}, fonts: {} };
export const darkTheme: Theme = { colors: {}, fonts: {} };
